# Environment variables for the application

# --- Database ---
# The connection string for your MySQL database.
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://user:password@localhost:3306/myapp"

# --- Security ---
# A secret key for signing JSON Web Tokens (JWTs).
# This should be a long, random, and secret string.
JWT_SECRET="your-super-secret-and-long-jwt-secret"

# --- Node.js Environment ---
# Set to 'development' for local development and 'production' for live environments.
NODE_ENV="development"